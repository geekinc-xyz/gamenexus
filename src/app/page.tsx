
// This page is no longer used directly because of the redirect in next.config.ts
// It's kept here to avoid build errors, but users will be sent to /games.
export default function HomePage() {
  return null;
}
